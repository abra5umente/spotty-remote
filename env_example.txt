# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Flask secret key (can be any random string)
SECRET_KEY=your_secret_key_here

# HTTPS Configuration
# Set to 'true' to enable HTTPS (required for Spotify production)
USE_HTTPS=false

# Domain name for HTTPS (optional, defaults to localhost)
DOMAIN_NAME=your-domain.com

# Certificate path (optional, defaults to ./certs)
CERT_PATH=./certs

# Port number (optional, defaults to 5000)
PORT=5000

# Redirect URI - Choose based on your setup:
# For HTTP (development): http://localhost:5000/callback
# For HTTPS (production): https://your-domain.com:5000/callback
# For Tailscale: https://your-tailscale-endpoint.ts.net:5000/callback
REDIRECT_URI=http://localhost:5000/callback 