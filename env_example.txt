# Spotify API Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Flask secret key (can be any random string)
SECRET_KEY=your_secret_key_here

# Port number (optional, defaults to 5000)
PORT=5000

# Note: This app uses the host's Tailscale installation
# Make sure Tailscale is running on your host machine:
# 1. Install Tailscale on your host
# 2. Run: tailscale up --authkey=your-auth-key
# 3. The app will automatically detect your Tailscale hostname
# 4. Access at: https://your-tailscale-hostname:5000 